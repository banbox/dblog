# 环境切换说明:
#   开发环境: cp .env.dev .env
#   测试环境: cp .env.test .env
#   生产环境: cp .env.prod .env

# =============================================================================
# Blockchain Configuration
# =============================================================================

# BlogHub Contract Address
PUBLIC_BLOG_HUB_CONTRACT_ADDRESS=0xCf7Ed3AccA5a467e9e704C703E8D87F634fB0Fc9

# Session Key Manager Contract Address
PUBLIC_SESSION_KEY_MANAGER_ADDRESS=0x5FbDB2315678afecb367f032d93F642f64180aa3

# RPC URL for blockchain interactions  https://chainlist.org/
PUBLIC_RPC_URL=http://localhost:8545

# Chain ID
# - 31337: Anvil (本地)
# - 11155420: Optimism Sepolia (测试网)
# - 10: Optimism Mainnet (主网)
PUBLIC_CHAIN_ID=31337

# =============================================================================
# Storage Configuration (Irys/Arweave)
# =============================================================================

# Irys Network: 'mainnet' (permanent storage) or 'devnet' (test, ~60 days)
PUBLIC_IRYS_NETWORK=devnet

# Arweave Gateways (comma-separated, first one is primary)
PUBLIC_ARWEAVE_GATEWAYS=https://gateway.irys.xyz,https://arweave.net,https://arweave.dev

# =============================================================================
# SubSquid GraphQL API
# =============================================================================

# SubSquid GraphQL endpoint for querying indexed blockchain data
PUBLIC_SUBSQUID_ENDPOINT=http://localhost:4350/graphql

# =============================================================================
# Gas Fee Configuration (for Session Key and Irys funding)
# =============================================================================

# Minimum gas fee multiplier (fund amount must be at least this * estimated gas)
# Default: 10 (10x gas fee)
PUBLIC_MIN_GAS_FEE_MULTIPLIER=10

# Default gas fee multiplier (actual fund amount = this * estimated gas)
# Default: 30 (30x gas fee)
PUBLIC_DEFAULT_GAS_FEE_MULTIPLIER=30
